[[section-system-scope-and-context]]
== Context and Scope

// TODO: Describe what is inside the system boundary (PROJECT_NAME) and what is outside it.
// Identify all external systems, users, and data flows.
// The System Context diagram below is rendered directly from workspace.dsl via Kroki.io.

=== Business Context

// The C4 System Context diagram is rendered by Kroki.io from your workspace.dsl.
// No local tool or Docker is required — the DSL is sent to kroki.io at build time.
// To use a self-hosted Kroki instance, set :kroki-server-url: in arc42/_config.adoc.

[structurizr]
....
include::../workspace.dsl[]
// view: SystemContext
....

[%autowidth, cols="^1,4"]
|===
| Symbol | Meaning

| 🟦 Blue box (Person) | A human user or role that interacts with the system
| 🟦 Blue box (System) | The software system being described
| ⬜ Grey box | An external software system or third-party dependency
| → Arrow | A relationship or data flow between elements
|===

// TODO: Annotate the diagram or describe the key actors and external systems below.

=== Technical Context

// TODO: Describe the technical interfaces between PROJECT_NAME and external systems.
// Include protocols, data formats, and communication channels.

[options="header", cols="1,2,3"]
|===
|Interface | Direction | Description
| [Interface 1] | Inbound  | [What data is received and from whom]
| [Interface 2] | Outbound | [What data is sent and to whom]
|===
